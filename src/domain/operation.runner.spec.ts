import { OperationRunner } from './operation.runner';

describe(OperationRunner.name, () => {
  describe('given no changes', () => {
    it('should finish early', () => {

    })
  })

  describe('given changes', () => {
    
  })
})